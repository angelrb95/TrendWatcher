services:
  stradivarius-monitor:
    build: .
    container_name: stradivarius-monitor
    restart: unless-stopped
    ports:
      - "${HOST_PORT:-8081}:8080"
    env_file:
      - .env
    volumes:
      - ./data:/data
