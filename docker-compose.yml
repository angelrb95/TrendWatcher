services:
  stradivarius-monitor:
    build: .
    container_name: stradivarius-monitor
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./data:/data
